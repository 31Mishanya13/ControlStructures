#include <iostream>
using namespace std;

//#define RECTANGLE
//#define TRIANGLE_1
//#define TRIANGLE_2
//#define TRIANGLE_3
//#define TRIANGLE_4
//#define ROMB
//#define RECTANGLE + -



void main()
{
	setlocale(LC_ALL, "ru");
	int n;
	cout << "Введите размер фигуры: "; cin >> n;

#ifdef RECTANGLE
	for (int i = 0; i < n; i++)
	{
		for (int i = 0; i < n; i++)
		{
			cout << "* ";
		}
		cout << endl;
	}
	// Данный код выводит на экран прямоугольник
	// Это цикл for для вывода строк с символом звездочки
    // Переменная i идет от 0 до n-1
    // Вложенный цикл также выполняет n итераций, выводя символ звездочки на каждой итерации
    // После каждой строки выводится символ переноса строки (endl) для перевода курсора на следующую строку
#endif // RECTANGLE

#ifdef TRIANGLE_1
	for (int i = 0; i < n; i++)
	{
		for (int j = 0; j <= i; j++)
		{
			cout << "* ";
		}
		cout << endl;
	}
	   //Данный код выводит на экран треугольник из звездочек, с каждой строкой происходит увеличение на одну звездочку(символ)
	   //Внешний цикл for проходится от i = 0 до i = n - 1, где n - это количество строк в треугольнике.
	   //Внутренний цикл for проходится от j = 0 до j = i, где i - это номер текущей строки.Внутри второго цикла выводится звездочка с пробелом после нее.
	   //После вывода всех звездочек в строке, происходит переход на новую строку с помощью оператора endl.
#endif //TRIANGLE_1

#ifdef TRIANGLE_2
	for (int i = 0; i < n; i++)
	{
		for (int j = i; j < n; j++)
		{
			cout << "* ";
		}
		cout << endl;
	}
	  // Данный код выводит на экран треугольник из звездочек, с каждой строкой происходит уменьшение на одну звездочку(символ)
      // Внешний цикл управляет количеством строк (высотой).
      // Внутренний цикл управляет количеством звездочек в каждой строке.
      // За каждую итерацию внутреннего цикла будет напечатана одна звездочка и пробел.
      // Перед переходом на новую строку мы убеждаемся, что текущая строка полностью напечатана.
#endif //TRIANGLE_2

#ifdef TRIANGLE_3
	for (int i = 0; i < n; i++)
	{
		for (int j = 0; j < i; j++) cout << "  ";			
		for (int j = i; j < n; j++) cout << "* ";			
		cout << endl;
	}
	   // Данный код выводит треугольник из звездочек с каждой строкой, содержащей на одну звездочку больше, чем предыдущая
	   // Оператор for задает цикл, который будет выполняться n раз, где n - заданное значение.
	   // Внутри цикла for выполняется цикл, который также будет выполняться n раз.
	   // В этом цикле выводится пустая строка на каждой итерации.
	   // В следующем цикле для каждой строки выводятся звездочки с увеличивающимся количеством на каждом шаге до n.
	   // В конце каждой строки выводится символ новой строки.
	   
#endif //TRIANGLE_3

#ifdef TRIANGLE_4
	for (int i = 0; i < n; i++) // цикл по строкам
	{ 
		for (int j = n - 1; j > i; j--) // цикл для вывода пробелов перед звездочками
		{ 
			cout << "  ";
		}
		for (int k = 0; k <= i; k++) // цикл для вывода звездочек в строке
		{ 
			cout << "* ";
		}
		cout << endl; 
}

#endif //TRIANGLE_4

#ifdef ROMB
	for (int i = 0; i < n; i++)
	{
		for (int j = i; j < n; j++)cout << " "; cout << "/";
		for (int j = 0; j < i; j++)cout << "  "; cout << "\\";
			cout << endl;
	}
	for (int i = 0; i < n; i++)
	{
		for (int j = 0; j <= i; j++)cout << " "; cout << "\\";
		for (int j = i + 1; j < n; j++)cout << "  "; cout << "/";
		cout << endl;
	}
	// данный код рисует ромб с диагональю из символов "/" и "\", используя два цикла
    // первый цикл рисует верхнюю часть ромба, начиная с одного символа "/" и увеличивая количество символов на один на каждой новой строке
    // второй цикл рисует нижнюю часть ромба, начиная с одного символа "\" и уменьшая количество символов на один на каждой новой строке
    // внутри каждого цикла происходит вывод символов и пробелов с помощью оператора cout.
#endif //ROMB

#ifdef RECTANGLE + -
	for (int i = 0; i < n; i++)
	{
		for (int j = 0; j < n; j++)
		{
			if ((i + j) % 2 == 0) cout << "+ ";
			else cout << "- ";
		}
		cout << endl;
     	//Данный код создает двухмерную шахматную доску размером n x n,  
		//где каждое поле помечено символами "+" или "-".
		//Если сумма индексов строки и столбца является четным числом,
		//то на поле ставится "+", в противном случае - "-".

			//Пример вывода для n = 4:

		    // + - + -
			// - + - +
			// + - + -
			// - + - +

		//Первый цикл(for) перебирает все строки(i), а второй цикл(for) перебирает все столбцы(j) на каждой строке.
		//Условие if проверяет, является ли сумма индексов(i + j) четным числом, 
		//и в зависимости от этого выводит на консоль символ "+" или "-".

			
#endif //RECTANGLE + -
}
	return 0;

}


